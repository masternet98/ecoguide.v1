# Python 가상환경
venv/
env/

# 환경변수 파일
.env

# Python 캐시
__pycache__/
*.py[cod]
*.pyo

# Jupyter Notebook
.ipynb_checkpoints/

# 로그 파일
*.log
*.jsonl

# pytest, mypy 캐시
.pytest_cache/
.mypy_cache/

# VSCode, PyCharm 등 에디터 설정
.vscode/
.idea/

# macOS
.DS_Store

# 배포/빌드
build/
dist/
*.egg-info/

# 데이터 파일 - 프롬프트는 Git에 포함, 일시적 파일은 제외
# data/ (전체 무시 제거 - 프롬프트 파일이 필요함)
data/*.cache
data/*.tmp
data/**/*.cache
data/**/*.tmp
data/prompts/backups/  # 백업 폴더만 제외

# 업로드 디렉토리 - 선택적 Git 관리
# 라벨 데이터는 Git 추적, 사용자 이미지는 제외
uploads/user_images/    # 사용자 업로드 이미지 (로컬/배포 임시 저장소)
uploads/temp/           # 임시 파일
uploads/cache/          # 캐시 파일
uploads/dong_cache/     # 동 단위 캐시 (빌드 중 생성)
uploads/*.tmp
uploads/*.cache
uploads/**/*.tmp
uploads/**/*.cache

# 라벨 데이터는 Git에 포함됨 (uploads/labels/_index.json 및 *.json)
# 행정구역 데이터는 Git에 포함됨 (uploads/districts/*.json)
# 모니터링 데이터는 Git에 포함됨 (uploads/monitoring/*.json)
# 폐기물 정보는 Git에 포함됨 (uploads/waste_types/*.json, waste_links/*.json)

# Downloaded Models
models/
yolov8n.pt
yolov8n-hands.pt

# Streamlit specific
# .streamlit/

# Temporary files
*.tmp
tmp/
temp/

# Development documentation (일시적)
instructions/remove/

# Claude Code specific
.claude/
uploads/dong_cache/
uploads/dong_cache
