# Python 가상환경
venv/
env/

# 환경변수 파일
.env

# Python 캐시
__pycache__/
*.py[cod]
*.pyo

# Jupyter Notebook
.ipynb_checkpoints/

# 로그 파일
*.log
*.jsonl

# pytest, mypy 캐시
.pytest_cache/
.mypy_cache/

# VSCode, PyCharm 등 에디터 설정
.vscode/
.idea/

# macOS
.DS_Store

# 배포/빌드
build/
dist/
*.egg-info/

# 데이터 파일 - 프롬프트는 Git에 포함, 일시적 파일은 제외
# data/ (전체 무시 제거 - 프롬프트 파일이 필요함)
data/*.cache
data/*.tmp
data/**/*.cache
data/**/*.tmp
data/prompts/backups/  # 백업 폴더만 제외

# 업로드 디렉토리 - 사용자 관리 데이터 제외
# 사용자가 업로드한 데이터(이미지+라벨)는 로컬에서만 관리
uploads/user_images/    # 사용자 업로드 이미지
uploads/labels/         # 사용자 라벨링 메타데이터 (이미지 관리용)
uploads/temp/           # 임시 파일
uploads/cache/          # 캐시 파일
uploads/dong_cache/     # 동 단위 캐시 (빌드 중 생성)
uploads/*.tmp
uploads/*.cache
uploads/**/*.tmp
uploads/**/*.cache

# Git에 포함되는 데이터 (시스템/행정 데이터)
# - uploads/districts/*.json       (행정구역 마스터 데이터)
# - uploads/monitoring/*.json      (시스템 모니터링 데이터)
# - uploads/waste_types/*.json     (폐기물 분류 마스터 데이터)
# - uploads/waste_links/*.json     (폐기물 처리 링크 마스터 데이터)
#
# Git에서 제외되는 데이터 (사용자 업로드 데이터)
# - uploads/labels/                (사용자 라벨링 데이터)
# - uploads/user_images/           (사용자 이미지 파일)

# Downloaded Models
models/
yolov8n.pt
yolov8n-hands.pt

# Streamlit specific
# .streamlit/

# Temporary files
*.tmp
tmp/
temp/

# Development documentation (일시적)
instructions/remove/

# Claude Code specific
.claude/
uploads/dong_cache/
uploads/dong_cache
