# Core dependencies
streamlit>=1.33.0
openai>=1.40.0
pillow>=10.0.0
qrcode[pil]>=7.4.2

# Vision pipeline (optional; see GEMINI.md for detailed CPU/GPU install instructions)
numpy>=1.24.0
rembg>=2.0.0
onnxruntime>=1.16.0
ultralytics>=8.0.0
mediapipe>=0.10.0
opencv-python>=4.7.0

# PyTorch is required by some vision backends (ultralytics, optional GPU)
# Install PyTorch separately following instructions in GEMINI.md to choose CPU vs GPU build.
# Example (PowerShell) for CPU:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Example (PowerShell) for CUDA-enabled GPU (e.g., cu118):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
