# Core
streamlit>=1.33.0,<1.49
openai>=1.40.0,<2.0
pillow>=10.0.0,<11.0
qrcode[pil]>=7.4.2
numpy>=1.24.0,<2.0
python-dotenv>=0.21.0
pandas>=2.0.0,<3.0
requests>=2.28.0,<3.0
beautifulsoup4>=4.11.0,<5.0

# Vision stack (Py3.11 verified)
# rembg==2.0.57
# onnxruntime==1.16.3
# ultralytics==8.0.20
# mediapipe==0.10.14
# opencv-python-headless==4.11.0.86

# (Optional) PyTorch for ultralytics / deep learning backends
# ⚠️ Streamlit Cloud 무료플랜은 CPU-only 빌드 권장
# 설치는 아래 중 하나 선택해서 로컬/Cloud에서 따로 실행
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
